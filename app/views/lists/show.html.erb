<h2><%= @list.name %></h2>

<ul>
  <% @list.bookmarks.each do |bookmark| %>
    <li><%=bookmark.movie.title %></li>
  <% end %>
</ul>


<p><%= link_to("Add new movie", new_list_bookmark_path(@list))%></p>
<p><%= link_to("Back to genre", root_path(@list))%></p>

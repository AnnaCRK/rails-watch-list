<h4>Add new genre</h4>

<%= simple_form_for [@list] do |f| %>
  <%= f.input :name %>
  <%= f.submit "Create genre", class: "btn btn-primary"%>
<% end %>

<p><%= link_to("Go to genres", root_path)%></p>
